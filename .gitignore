*.pem
*.key
.env

# Excluded deliverables (not pushed)
Nova Health Tech's challenge.txt
Task List - Task list.csv

# Kiro / IDE
.vscode/
.kiro/
.idea/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# SAM / AWS
.aws-sam/

# OS
.DS_Store
Thumbs.db

# Node
node_modules/

# Large third-party corpora — fetch at runtime, don't commit
data/pubmed/pubmed_medrag_sample.jsonl
data/pubmed/*.jsonl

# EC2 deploy artifacts
aws-demo/ec2/.outputs.env
aws-demo/ec2/nova-app.tar.gz

# Ad-hoc test payloads
scripts/_test_*.json

# Internal Nova documents — do not commit
data/clinical-trials/protocols/

# Department PMC reference PDFs — re-downloadable via
# `python scripts/download_department_refs.py`. Keep the README for
# reviewers, drop the actual PDFs so the repo stays small.
data/clinical-trials/departments/*/*.pdf

# User's local Alibaba Cloud reference downloads (copyrighted vendor docs)
reference/

# POC build artifacts (FAISS indexes, Lambda zips)
poc/build/
poc/*.zip
/tmp/poc-build-*/

# Cloud access key exports (never commit secrets)
AccessKey.csv
*AccessKey*.csv

# WHO and PubMed source PDFs — large, re-downloadable, not for repo
data/who/*.pdf
data/pubmed/
data/clinical-trials/protocols/*.pdf

# POC runtime outputs and secrets
poc/aws_claude/.outputs.env
poc/aws_claude/.managed_outputs.json
poc/aws_claude/.test_results.json
poc/aws_claude/nova-claude-app.tar.gz

# Recovered chat history — local only
recovered_chat/
RECOVERED_CHAT_SUMMARY.md

# Temp scripts for kiro chat recovery
scripts/dump_kiro_chat.py
scripts/export_kiro_chat.py
scripts/find_kiro_workspace.py

# Temp cleanup files
tmp_policy.json
poc/aws_claude/check_agent.py
