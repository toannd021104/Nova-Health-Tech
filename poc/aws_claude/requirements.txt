# PoC runtime — AWS + Claude (Version A).
# Uses Bedrock KB (OpenSearch Serverless + Neptune GraphRAG) — no local FAISS.
boto3>=1.40,<2.0
fastapi>=0.115,<1.0
pydantic>=2.9,<3.0
uvicorn[standard]>=0.30
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4
langgraph>=0.2,<0.4
pypdf>=4.0,<6.0
numpy>=1.26,<3.0
# Redis OSS client — only used if REDIS_ENDPOINT is set (disabled in PoC).
redis>=5.0,<6.0
