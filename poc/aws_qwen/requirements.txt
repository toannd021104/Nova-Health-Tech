# POC runtime — AWS + Qwen (Version B).
boto3>=1.40,<2.0
fastapi>=0.115,<1.0
pydantic>=2.9,<3.0
uvicorn[standard]>=0.30
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4
langgraph>=0.2,<0.4
faiss-cpu>=1.8,<2.0
pypdf>=4.0,<6.0
numpy>=1.26,<3.0
# ElastiCache Redis OSS client (NOT Valkey)
redis>=5.0,<6.0

# Fine-tuning pipeline (install separately if not running training)
# pip install -r requirements-finetune.txt
